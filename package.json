{
  "name": "logger",
  "version": "1.0.0",
  "description": "Simple logger",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist",
    "build": "npm run clean && tsc",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "goodbyte-uy",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.6.2"
  }
}
